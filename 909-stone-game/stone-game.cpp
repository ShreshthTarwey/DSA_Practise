class Solution {
public:
    // int recFun(vector<int>& piles, int i, int j){
    //     if(i>j){
    //         return 0;
    //     }
    //     int take_i = piles[i] + min(recFun())
    // }
    bool stoneGame(vector<int>& piles) {
        // int p1 = recFun(piles, 0, piles.size()-1);
        // int sum = accumulate(piles.begin(), piles.end(), 0);
        // int p2 = sum - p1;
        // return p1>=p2;
        return true;
    }
};